"""
Task 1: Neural Network with Manual Backpropagation.

Everything in this package is implemented with NumPy only. No autograd and no
`.backward()` from any framework is used here, as required by the task brief.
`torch` appears only in the correctness harness at the repository root.
"""
